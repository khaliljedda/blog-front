pipeline {
    agent any
  
    stages {
        stage('test') {
            steps {
                sh('npm --version')
            }
        }
         stage('build') {
            steps {
                sh('echo "khalil"')
            }
        }
         stage('deploy') {
            steps {
                sh('npm --version')
            }
        }
    }
}